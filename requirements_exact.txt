# Kronos Trading System - Exact Pip Lockfile
# AUDITED Phase 1 - 2026-08-10
# Consistent with environment.yml - Same torch version 2.4.1, verified compatible
# Kronos repo requires torch>=2.0.0, we pin 2.4.1 for reproducibility
# For CUDA 12.1 (RTX 3060): install torch separately via --index-url https://download.pytorch.org/whl/cu121

# === Python Core Note ===
# Python 3.10.13 recommended - see environment.yml

# === PyTorch - PINNED & VERIFIED ===
# Install with:
# pip install torch==2.4.1 torchvision==0.19.1 torchaudio==2.4.1 --index-url https://download.pytorch.org/whl/cu121
# For CPU fallback:
# pip install torch==2.4.1 torchvision==0.19.1 torchaudio==2.4.1 --index-url https://download.pytorch.org/whl/cpu
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1

# === Kronos Official (from upstream requirements.txt) ===
# Source: https://github.com/shiyu-coder/Kronos/blob/master/requirements.txt
# Verification: all import tests pass
numpy==1.26.4
pandas==2.2.2
einops==0.8.1
huggingface-hub==0.33.1
matplotlib==3.9.3
tqdm==4.67.1
safetensors==0.6.2

# === Exchange Layer - CCXT ONLY per Audit #4 ===
# python-binance REMOVED - Reason: CCXT already supports Binance spot/testnet/mainnet via unified API
# Using both would duplicate, increase attack surface, cause version conflicts
# Exception: None needed - CCXT covers all Binance functionality required
# Concrete justification: CCXT provides fetch_ohlcv, create_order, load_markets, testnet via options dict
ccxt==4.2.25
websocket-client==1.8.0
yfinance==0.2.40

# === Data & DB ===
SQLAlchemy==2.0.31
PyYAML==6.0.1
python-dotenv==1.0.1
pydantic==2.8.2
loguru==0.7.2
requests==2.32.3
scipy==1.13.1
scikit-learn==1.5.0
seaborn==0.13.2

# === Backtesting ===
vectorbt==0.26.0
quantstats==0.0.64
pyqlib==0.9.6
timm==1.0.9

# === Dashboard ===
streamlit==1.37.1
plotly==5.24.1
schedule==1.2.1
python-telegram-bot==21.4
jupyter==1.1.1
ipykernel==6.29.5

# === Consistency Verification ===
# Run: python scripts/setup/environment_report.py to verify all versions match environment.yml
# Expected: torch 2.4.1, numpy 1.26.4, pandas 2.2.2, etc.
