[pytest]
# Test only this repository's suite; Kronos/ is vendored upstream source.
# phase9a/ and execution/ are the Phase 9A analysis + paper execution modules.
testpaths = tests phase9a execution
# Ensure the repo root is importable so `from phase9a...`/`from execution...`
# resolve regardless of how pytest is invoked.
pythonpath = .
